### 现状分析
项目已有很多适合“人类阅读”的优秀文档。

### AI 开发痛点
AI 每次初始化任务时，无法一次性读取全部人类文档（且 Token 成本高）。

### 改进建议
在项目根目录设立专门给 AI 阅读的指令文件（如 `GEMINI.md` 或 `AI_RULES.md`）。内容应包含硬性约束，例如：“禁止使用拼接 SQL，必须用参数化查询”、“遇到错误必须添加 Electron-log”、“文件最大行数限制等”。AI 在任何任务开始时默认加载此文件，保证维护的一致性。