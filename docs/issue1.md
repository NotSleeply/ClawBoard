### 现状分析
在目前的 Roadmap 中，单元测试和 E2E 测试被安排在了 Phase 4，而 Phase 1-3 涉及大量数据库替换和系统级重构。

### AI 开发痛点
人类开发者可能凭借经验避免回归错误，但纯 AI 维护修改代码如果没有自动化测试作为“红绿灯”进行约束，极易在重构中破坏现有功能。

### 改进建议
强制采用 TDD（测试驱动开发）或先补充核心层（如 `Database`, `ClipboardWatcher`）的测试用例。AI 必须依赖 `pnpm test` 的输出来验证其重构没有引发回归。